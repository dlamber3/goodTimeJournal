won't user need the id to connect to their journals?

CREATE DATABASE Good_Time;

\c Good_Time

CREATE TABLE User (
	username TEXT NOT NULL,
	password TEXT NOT NULL,
	email TEXT,
	id NUMBER,
	PRIMARY KEY (id)

);

CREATE TABLE Journal (
	U_ID INTEGER REFERENCES Journal_Entries (U_ID),
	entry TEXT
);

CREATE TABLE Journal_Entries (
	category BOOLEAN,
	date DATE,
	entry TEXT,
	last_modified DATE,
	U_ID INTEGER PRIMARY KEY,
	Activity_Level INTEGER,
	published BOOLEAN,
);